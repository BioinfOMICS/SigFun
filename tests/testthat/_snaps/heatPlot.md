# heatPlot handles errors correctly

    Code
      heatPlot(GSE181574.sigfun, dotSize = "invalid_method")
    Condition
      Error in `match.arg()`:
      ! 'arg' should be one of "pval", "p.adjust"

