% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sigCor.R
\name{sigCor}
\alias{sigCor}
\title{Signature-Transcriptome Correlation Analysis}
\usage{
sigCor(SE_data, cor.method = "spearman", Z.transform = FALSE)
}
\arguments{
\item{SE_data}{A \code{SummarizedExperiment} object containing:
\itemize{
\item \code{assays}: Gene expression matrix (genes in rows, samples in columns)
\item \code{colData}: Signature scores/classifications for each sample
\item \code{rowData}: Gene annotation with ENSEMBL IDs and symbols
}}

\item{cor.method}{Correlation method:
\itemize{
\item "spearman" (default): Non-parametric rank correlation
\item "pearson": Linear correlation
\item "kendall": Rank correlation for small datasets
\item "logit": Logistic regression for binary signatures (e.g., high/low risk)
}}

\item{Z.transform}{Logical. Whether to z-score normalize expression data:
\itemize{
\item \code{FALSE} (default): No transformation (recommended for binary signatures)
\item \code{TRUE}: Standardize expression values (recommended for continuous
signatures)
}}
}
\value{
Returns a modified \code{SummarizedExperiment} object with:
\itemize{
\item \code{cor.df} in metadata: Dataframe containing correlation statistics with
columns:
\itemize{
\item \code{gene}: ENSEMBL ID
\item \code{cor}: Correlation coefficient (or log-odds for logit)
}
}
}
\description{
Calculates genome-wide correlations between a clinical signature
and gene expression profiles. This core function of the SigFun package
bridges phenotypic signatures with transcriptomic data to enable downstream
functional analysis.
}
\details{
This function performs three key operations:
\enumerate{
\item Integrates signature data with expression matrices
\item Computes genome-wide correlations using specified method
\item Stores results in SE object for downstream analysis with sig2GSEA
}
}
\examples{
# Load demo data
data("demo_GSE181574")

# Binary signature analysis (MammaPrint high/low risk)
SE_cor_logit <- sigCor(
  SE_data = SE_GSE181574,
  cor.method = "logit",
  Z.transform = FALSE
)

# Continuous signature analysis (e.g., risk score)
SE_cor_spearman <- sigCor(
  SE_data = SE_GSE181574,
  cor.method = "spearman",
  Z.transform = TRUE
)

# Access results
metadata(SE_cor_logit)$cor.df
}
