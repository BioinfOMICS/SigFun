% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bar.R
\name{plot_bar}
\alias{plot_bar}
\title{Visualization of Enriched Pathways via Bar Plot}
\usage{
plot_bar(
  SE_data.fgsea,
  significat_type = "pval",
  topN = 10,
  strings = c("GOBP", "GOCC", "GOMF", "KEGG", "REACTOME", "WP")
)
}
\arguments{
\item{SE_data.fgsea}{A \code{SummarizedExperiment} object containing:
\itemize{
\item \code{fgseaRes} in metadata: GSEA results from \code{sig2GSEA}
\item \code{cor.df} in metadata: Correlation statistics from \code{sigCor}
}}

\item{significat_type}{Character. Significance metric for filtering:
\itemize{
\item "pval" (default): Use nominal p-values
\item "qval": Use false discovery rate adjusted q-values
}}

\item{topN}{Integer. Number of top significant pathways to display per
category (default=10)}

\item{strings}{Character vector. Pathway categories to visualize
(e.g. "HALLMARK", "KEGG"). Default includes major MSigDB collections.}
}
\value{
Returns a list of ggplot objects containing bar plots with:
\itemize{
\item Pathways ordered by significance (most significant first)
\item Bar height representing -log10(p/q-value)
\item Fill color indicating association direction (red = positive, blue = negative)
\item Horizontal layout optimized for pathway name readability
}
}
\description{
Generates bar plots displaying significant biological pathways
associated with a gene signature, ordered by enrichment significance and
colored by association direction. This visualization complements the heatmap
output to provide a focused view of top pathway associations.
}
\details{
Key features:
\enumerate{
\item Automatically handles empty results by returning informative empty plots
\item Filters pathways using both statistical significance and string matching
\item Integrates seamlessly with SigFun workflow outputs
}
}
\examples{
\dontrun{
# After running sig2Fun analysis
data("demo_GSE181574")
res <- sig2Fun(SE_GSE181574, strings = "HALLMARK")

# Generate bar plots for HALLMARK pathways
bp <- plot_bar(
  SE_data.fgsea = res,
  significat_type = "pval",
  topN = 10,
  strings = "HALLMARK"
)

# Display plot
bp$HALLMARK
}

}
