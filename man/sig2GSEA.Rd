% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sig2GSEA.R
\name{sig2GSEA}
\alias{sig2GSEA}
\title{Gene Set Enrichment Analysis for Signature Interpretation}
\usage{
sig2GSEA(SE_data.cor, ranking.method, pathways.all, NPROC = 1)
}
\arguments{
\item{SE_data.cor}{A \code{SummarizedExperiment} object containing correlation results from \code{sigCor}. Must have:
\itemize{
\item \code{cor.df} in metadata: Dataframe with correlation statistics (from \code{sigCor} output)
\item \code{rowData}: Gene annotation mapping (ENSEMBL IDs to symbols)
}}

\item{ranking.method}{Character. Gene ranking method:
\itemize{
\item "stat" (default): Use correlation statistics
\item "pval": Use p-values from correlation analysis
}}

\item{pathways.all}{List. Pathway database containing gene sets and associated genes.
Typically obtained via \code{fgsea::gmtPathways} from MSigDB files (e.g., msigdb.v2023.1.Hs.symbols.gmt)}

\item{NPROC}{Integer. Number of processors for parallel computation (default=1 for serial processing)}
}
\value{
Returns an augmented \code{SummarizedExperiment} object with:
\itemize{
\item \code{fgseaRes} in metadata: Dataframe containing GSEA results with columns:
\itemize{
\item pathway: Pathway name
\item pval: Nominal p-value
\item padj: Adjusted p-value (FDR)
\item ES: Enrichment score
\item NES: Normalized enrichment score
\item size: Number of genes in pathway
\item leadingEdge: Vector of leading edge genes
}
\item \code{DESeq.ranks} in metadata: Named vector of ranked genes
\item \code{cor.df} preserved from input
}
}
\description{
Performs pathway enrichment analysis using correlation statistics from signature-transcriptome associations. This core component of the SigFun package enables systematic identification of biological functions associated with gene signatures through GSEA methodology. The function processes SigCor results to rank genes and performs enrichment against specified pathway databases.
}
\details{
This function bridges signature-transcriptome correlations with biological interpretation by:
\enumerate{
\item Extracting correlation statistics from SigCor results
\item Creating gene rankings based on specified metric (statistics or p-values)
\item Performing GSEA using the fgsea algorithm
\item Storing results in SE object for downstream visualization
}
}
\examples{
\dontrun{
# Load demo data
data("demo_GSE181574")

# First run correlation analysis
SE_cor <- sigCor(SE_data = SE_GSE181574,
                cor.method = "logit",
                Z.transform = FALSE)

# Perform GSEA using correlation statistics
SE_gsea <- sig2GSEA(
  SE_data.cor = SE_cor,
  ranking.method = "stat",
  pathways.all = pathways.all,
  NPROC = 4
)

# Access results
metadata(SE_gsea)$fgseaRes  # GSEA results
metadata(SE_gsea)$DESeq.ranks  # Gene rankings

# Note: Warnings about p-value estimation are normal for highly significant pathways
}
}
